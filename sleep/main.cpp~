#include <os>
#include <iostream>
#include <thread>
#include <chrono>

void Service::start(){
  for(int i =0;i<10;i++)
  {
	  std::this_thread::sleep_for(std::chrono::seconds(1));}
  return;
  exit(EXIT_SUCCESS);
}
